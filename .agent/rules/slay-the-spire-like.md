---
trigger: always_on
---

コマンドはPowerShellのコマンドを使うようにしてください。
webでの確認はお願いしたときだけ、行ってください。

ステータス（バフ・デバフ）を追加した際は、必ず `src/core/entity.ts` の `DEBUFF_TYPES` または `BUFF_TYPES` 定数リストにも追加してください。

リファクタリングの指示がない場合には、関数の記載場所の移動などは行わず、なるべく追記・変更の形で修正を行うこと。

コード品質向上のため、極力 `any` 型を使用せず、適切な型定義を行ってください。既存の `any` がある場合は、修正のタイミングで型定義を行うようにしてください。